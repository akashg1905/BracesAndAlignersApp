<?xml version="1.0" encoding="utf-8"?>
<!-- Release / default: HTTPS only. Debug overlays may re-enable cleartext for local backends. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
